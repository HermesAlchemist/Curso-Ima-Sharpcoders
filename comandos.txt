COMANDOS GIT
git config --global user.name "Heitor Lemos"
git config --global user.email "heitorlemos@gmail.com"
git init
git add .
git add (filename)
git rm --cached (filename)
git status
git commit -m "message"
git checkout (branchName)
git push -u origin main
git pull origin main